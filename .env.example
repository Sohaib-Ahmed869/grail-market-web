# Where the backend is. Baked into the bundle at build time, so a change here
# needs a rebuild, not just a restart.
#
# local:      http://localhost:8180
# deployed:   https://your-api-host
NEXT_PUBLIC_API_URL=http://localhost:8180
